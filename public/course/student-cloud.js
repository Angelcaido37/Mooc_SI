(()=>{
  const toast=m=>{const t=document.querySelector('#toast');if(t){t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3000)}};
  window.addEventListener('nexus-auth-change',e=>{const {user,role,configured}=e.detail;if(!user){location.href='index.html';return}if(role==='teacher')toast('Está viendo el portal del estudiante como docente.');const note=document.querySelector('.privacy-note');if(note)note.textContent=configured?'Avance conectado a la nube. Las evidencias pueden sincronizarse con Classroom.':'Modo demostración: el avance permanece sólo en este dispositivo.'});
  document.querySelector('#studentSignOut')?.addEventListener('click',async()=>{await NEXUS_AUTH.signOut();location.href='index.html'});
  let uploaded=null;document.querySelector('#cloudEvidence')?.addEventListener('click',()=>document.querySelector('#evidenceDialog').showModal());
  document.querySelector('#uploadEvidence')?.addEventListener('click',async()=>{const file=document.querySelector('#evidenceFile').files[0],box=document.querySelector('#evidenceCloudResult');if(!file){box.innerHTML='<p class="feedback bad">Seleccione un archivo.</p>';return}try{box.innerHTML='<p>Guardando…</p>';uploaded=await NEXUS_AUTH.uploadEvidence(file,{evidenceId:document.querySelector('#evidenceId').value});box.innerHTML='<p class="feedback good">Evidencia guardada correctamente.</p>'}catch(e){box.innerHTML=`<p class="feedback bad">${e.message}</p>`}});
  document.querySelector('#turnInEvidence')?.addEventListener('click',async()=>{const box=document.querySelector('#evidenceCloudResult');if(!uploaded){box.innerHTML='<p class="feedback bad">Guarde primero la evidencia.</p>';return}try{await NEXUS_AUTH.callClassroom('submitEvidence',{courseId:document.querySelector('#classCourseId').value,courseWorkId:document.querySelector('#classWorkId').value,submissionId:document.querySelector('#classSubmissionId').value,evidenceUrl:uploaded.url});box.innerHTML='<p class="feedback good">Evidencia entregada en Classroom.</p>'}catch(e){box.innerHTML=`<p class="feedback bad">${e.message} La evidencia permanece guardada.</p>`}});
  const original=localStorage.setItem.bind(localStorage);localStorage.setItem=(key,value)=>{original(key,value);if(key==='nexus-progress'&&window.NEXUS_AUTH?.user)NEXUS_AUTH.saveProgress(JSON.parse(value)).catch(()=>{})};
})();
