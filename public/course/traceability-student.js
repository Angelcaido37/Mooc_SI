(()=>{
 const T=()=>window.NEXUS_TRACEABILITY;
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
 const confirmables=[1,2,16,17];
 function view(){return `<header class="page-head"><span class="eyebrow">CONSTANCIAS Y VOZ ESTUDIANTIL</span><h1>Mi trazabilidad académica</h1><p>Estas confirmaciones registran acceso u oportunidad; no significan que estés de acuerdo con el docente y nunca afectan tu calificación, XP o insignias.</p></header><section class="content-block ack-box"><h2>Confirmaciones disponibles</h2><p>Selecciona “No puedo confirmarlo” cuando corresponda. También puedes dejar una observación.</p>${T().indicators.filter(x=>confirmables.includes(x.id)).map(x=>`<article class="trace-card"><span class="trace-mode">Indicador ${x.id}</span><h3>${esc(x.name)}</h3><p>${esc(x.evidence)}</p><textarea id="ack-note-${x.id}" placeholder="Observación opcional"></textarea><div class="ack-actions"><button class="btn btn-primary" data-ack="${x.id}" data-response="confirm">Confirmo que tuve acceso/oportunidad</button><button class="btn btn-outline" data-ack="${x.id}" data-response="cannot_confirm">No puedo confirmarlo</button></div></article>`).join('')}</section><section class="content-block"><h2>Principio de protección</h2><p>La percepción sobre respeto, justicia, confianza o creatividad debe conservar espacio propio y, cuando se recolecte, debe poder analizarse separada de los acuses objetivos. Nexus no convierte una confirmación en “firma de conformidad”.</p></section>`}
 async function bind(){document.querySelectorAll('[data-ack]').forEach(b=>b.onclick=async()=>{const id=+b.dataset.ack,x=T().indicators.find(v=>v.id===id),note=document.querySelector(`#ack-note-${id}`)?.value||'';try{await window.NEXUS_AUTH.api('/api/acknowledgements',{method:'POST',body:JSON.stringify({indicatorId:String(id),sessionId:'course',statement:x.name,response:b.dataset.response,observation:note,resourceVersion:'NEXUS18',resourceHash:''})});b.textContent='✓ Registrado';b.disabled=true}catch(e){alert('No fue posible registrar: '+e.message)}})}
 window.NEXUS_TRACE_STUDENT={view,bind};
})();
