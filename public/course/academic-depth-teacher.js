(()=>{
 const D=()=>window.NEXUS_ACADEMIC_DEPTH?.lessons||[], C=()=>window.NEXUS_COURSE;
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
 const byId=id=>D().find(x=>x.id===id);
 function list(){return `<header class="page-head"><span class="eyebrow">PROFUNDIDAD ACADÉMICA</span><h1>Centro de preparación docente</h1><p>Cada lección conserva el guion operativo de clase y añade preparación disciplinar: conceptos, errores frecuentes, preguntas difíciles, transferencia, remediación y fuentes.</p></header><section class="content-block"><div class="feature-grid"><span class="feature-chip">✓ Qué debo dominar antes de entrar</span><span class="feature-chip">✓ Explicación ampliada</span><span class="feature-chip">✓ Errores frecuentes</span><span class="feature-chip">✓ Preguntas del alumnado</span><span class="feature-chip">✓ Transferencia y contraejemplos</span><span class="feature-chip">✓ Fuentes por unidad</span></div></section><section class="session-list">${C().units.map(u=>`<article class="session-card"><div><span class="eyebrow">MISIÓN ${u.number}</span><h2>${esc(u.title)}</h2><p>${esc(u.purpose)}</p><small>${u.lessons.length} lecciones · resultado: ${esc(u.outcomes[0])}</small></div><div>${u.lessons.map(l=>`<button class="btn btn-outline depth-link" data-depth="${l.id}">${esc(l.title)}</button>`).join('')}</div></article>`).join('')}</section>`}
 function detail(id){const x=byId(id);if(!x)return list();return `<header class="page-head"><span class="eyebrow">PREPARAR LA EXPLICACIÓN</span><h1>${esc(x.title)}</h1><p>${esc(x.objective)}</p></header><div class="button-row"><button class="btn btn-outline" data-depth-back>← Centro docente</button></div><section class="content-block depth-three"><article><span class="eyebrow">ANTES</span><h2>Conocimiento previo a activar</h2><p>${esc(x.prior)}</p></article><article><span class="eyebrow">DURANTE</span><h2>Explicación esencial</h2>${x.essential.map(p=>`<p>${esc(p)}</p>`).join('')}</article><article><span class="eyebrow">AMPLIAR</span><h2>Notas docentes</h2>${x.teacherExpansion.map(p=>`<p>${esc(p)}</p>`).join('')}</article></section><section class="cards"><article class="card"><h3>Errores frecuentes</h3><ul>${x.errors.map(v=>`<li>${esc(v)}</li>`).join('')}</ul></article><article class="card"><h3>Preguntas que pueden aparecer</h3><ul>${x.likelyQuestions.map(v=>`<li>${esc(v)}</li>`).join('')}</ul></article><article class="card"><h3>Cómo comprobar dominio</h3><p>${esc(x.mastery)}</p></article></section><section class="content-block"><h2>Secuencia de transferencia</h2><p><strong>Práctica guiada:</strong> ${esc(x.guided)}</p><p><strong>Práctica autónoma:</strong> ${esc(x.autonomous)}</p><p><strong>Problema nuevo:</strong> ${esc(x.transfer)}</p><p><strong>Si necesita refuerzo:</strong> ${esc(x.remediation)}</p><p><strong>Si ya domina:</strong> ${esc(x.advanced)}</p><h3>Fuentes base del curso</h3><ul>${x.references.map(r=>`<li>${esc(r)}</li>`).join('')}</ul></section>`}
 function bind(go){document.querySelectorAll('[data-depth]').forEach(b=>b.onclick=()=>{location.hash='depth/'+b.dataset.depth; window.dispatchEvent(new HashChangeEvent('hashchange'))});document.querySelector('[data-depth-back]')?.addEventListener('click',()=>{location.hash='depth';window.dispatchEvent(new HashChangeEvent('hashchange'))})}
 window.NEXUS_DEPTH_TEACHER={list,detail,bind};
})();
